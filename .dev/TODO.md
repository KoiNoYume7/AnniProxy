1. if scoop = true in the .config file, it must also install `scoop install git` for buckets
2. when installing scoop freshly via the batch file and the script then trying to install pwsh via scoop, it fails because scoop is not detected yet, needs reload
3. brave and cloudflared still in .bin folder, why?
---
1. make managing ssh keys easier and more automated pls.